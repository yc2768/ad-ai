[project]
name = "ad-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "fastapi>=0.136.1",
    "langchain>=1.3.1",
    "pydantic-settings>=2.13.0",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.47.0",
]

[project.scripts]
ad-ai = "main:main"
